# SemaLoom language

SemaLoom 描述企业业务的含义、事实来源、确定性判断与受控操作。以下词汇用于核心模型和各行业示例的共同语言。

## 业务语义

**ObjectType（对象类型）**：一类具有稳定身份的业务实体，例如纳税主体或采购订单。Object 是该类型的实例。

**ObjectIdentity（对象身份）**：在租户及对象类型内唯一标识一个对象的业务键集合；同名字段不构成相同身份。

**Property（属性）**：对象的描述性或状态性特征。带 `unit` 的数值属性是测量槽，对应表中的金额/数量列，不是另建一层科目清单。

**Metric（测量 IR）**：编译器从「带单位的数值属性 + Mapping」生成的查询入口，不是本体里要单独维护的一类业务概念。本体只描述对象、属性和关系。合计、平均是查询时的运算，由 Runtime 编译为来源 SQL，调用方不写 SQL。需要稳定业务别名或 EAV 科目名时，可用可选词条，不作为默认建模步骤。

**Grain（粒度）**：恰好确定一个指标观测值的维度组合及时间范围。

**Perspective（口径）**：一个业务测量的解释视角，例如申报口径或审计口径；相同名称的不同口径不能隐式替换。

**Link（关系）**：两个对象类型之间经过声明的业务关联，包含方向、身份对应与基数。

**Mapping（映射）**：将对象及其测量槽绑定到一张事实表或一个 API 的经审核声明。一表一条；不为每个科目或金额列复制 Mapping。

**Domain Pack（领域包）**：对象、关系、规则、政策、Action 与少量指标词条的业务语言，作为事实表之上的补充配置，独立于具体企业系统。

**Integration Binding（接入绑定）**：将领域定义绑定到来源协议的版本化声明，包括读取 Mapping 与写入 Action Binding；与领域包分别维护、组合审核发布。

**Adapter（接入适配器）**：实现协议中立接口的可信接入代码，拥有物理映射编译、连接认证、协议执行及结果归一化。按协议或系统能力复用，不包含业务判断规则。

## 事实与判断

**Observation（观测）**：在指定来源及业务上下文下获取某项业务值的结果，包括存在、缺失、空值或无法取得。

**SemanticFact（语义事实）**：来源、粒度、单位和取值均明确的已观测业务值；缺失不是值为零的事实。

**Rule（规则）**：从声明的输入按确定性逻辑产生业务值或判断的定义。

**Claim（判断）**：有明确命题及上下文的 TRUE、FALSE 或 UNKNOWN 结论；发现一条数据不自动证明任意命题为真。

**Claim Definition（命题定义）**：可以在不同业务上下文中重复评估的经审核命题；它的身份不等于某一次评估结果的身份。

**Policy（业务政策）**：决定在某个适用范围和业务时间采用哪个规则版本的业务定义。
_Avoid_：用 Policy 同时指代访问控制策略。

**Authorization Policy（授权策略）**：决定某个主体能否对指定资源进行某项操作以及允许的数据范围的规则。

**Business Time（业务时间）**：事实或业务政策适用的时间或期间。

**Knowledge Time（认知时间）**：某项来源版本或政策版本被系统记录、发布或选定的时间。

**Evidence（证据）**：支持一个结果的来源、输入、转换、规则版本及执行关系。

**Audit Event（审计事件）**：记录何人在何时以何权限发起或改变了某项操作的事件；它本身不证明业务判断正确。

## 操作与发布

**Action（操作）**：会改变业务系统状态的业务能力定义。

**Action Plan（操作计划）**：待审批的一次具体操作的目标、参数、前提与预期效果。

**Approval（审批）**：有权限的主体对特定操作计划在有效期内作出的批准或拒绝。

**Action Execution（操作执行）**：一个已批准计划对应的业务执行及其核验过程。

**Semantic Release（语义发布版本）**：一起审核并不可变发布的一组语义定义及其精确依赖。
_Avoid_：将可变的 latest 标签视为版本身份。

**Population（统计集合）**：在明确授权范围、指标口径、统计单位、年度与筛选条件下完整选定的对象集合。对象枚举完整不代表来源已复核或代表全部行业；缺失值处理、重复统计单位和比较分母必须显式定义。
