# 0004 — 操作审批绑定具体计划，不承诺通用 exactly-once

状态：accepted。

一次审批绑定目标、参数摘要、租户、发起者、语义发布版本、环境/执行 profile、前提版本和失效时间。执行前重新鉴权并检查前提；影响合法性的目标版本条件由下游与写入原子检查，操作使用持久化执行记录及下游幂等协议。下游结果不明时进入待核对状态，禁止通过新请求号盲目重发写入。

与“Runtime 生成一个幂等键就足够”的方案相比，这需要下游配合与崩溃恢复测试；但只有它能区分未执行、执行失败和执行成功但响应丢失。Runtime 本地去重无法单独消除跨系统重复副作用。
