# 0002 — 观测、判断与执行错误分离

状态：accepted。

Claim 使用三值逻辑，Observation 单独表达取值是否可得，操作失败使用类型化诊断。零行仅能在完整且有权威性的可见范围内否定一个明确的存在性命题；不能把缺少金额转换为零，或把鉴权失败转换成“不存在”。

采用显式类型和严格规则输入预检，代替将所有异常压入 UNKNOWN 的单一枚举。其代价是响应包含多个正交状态，但调用方可以区分数据缺口、业务否定与运行故障，不会将失败伪装成事实。
